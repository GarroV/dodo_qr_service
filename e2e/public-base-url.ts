/**
 * Публичный адрес продукта для сквозного прогона.
 *
 * Он намеренно НЕ совпадает с адресом, на котором поднят сервер прогона: код,
 * зашитый в наклейку, обязан приходить из окружения площадки, а не из адреса, по
 * которому открыта админка. Если однажды кто-то вернёт адрес в константу или
 * заставит его считаться по запросу, сценарий `e2e/qr.spec.ts` это увидит.
 *
 * Значение прокидывается серверу прогона в `playwright.config.ts`. Оно попадает и в
 * сборку, и в запуск: значения переменных запекаются сборкой (D033).
 */
export const E2E_PUBLIC_BASE_URL = "https://qr-e2e.example:10000";
