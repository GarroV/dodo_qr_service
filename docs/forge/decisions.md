<!-- managed by forge: do not change the row format below — forge-build and forge-status parse this file -->
# Decisions

<!-- id: Dnnn (D001, D002, ...). who: owner (the owner decided) | auto (decided autonomously by the dispatcher). -->

| id | date | decision | why | who |
|---|---|---|---|---|
<!-- | D001 | 2026-07-29 | Example decision | Example rationale | owner | -->
| D001 | 2026-09-05 | Заполняющего чек-лист не опознаём: ни входа, ни PIN, ни привязки устройства. Заполнить может любой, кто отсканировал QR | «не усложняем» — основная концепция продукта; опознание требует администрирования списков и раздачи доступов, чего на пилоте не окупается | owner |
| D002 | 2026-09-05 | История заполнений хранится двумя способами сразу: версии чек-листа неизменяемы, и каждое заполнение хранит снимок пунктов | «нам просто надо учесть что чек листы будут меняться, и история не должна перестраиваться вместе с ними» | owner |
| D003 | 2026-09-05 | GPS не запрашиваем, порогов скорости заполнения нет. В детализации видно только длительность заполнения | Гео в помещении даёт обратный по надёжности сигнал: честный сотрудник на кухне получает нарушение, нечестный из дома — чистые координаты. Пороги скорости ложно срабатывают на коротких чек-листах | owner |
| D004 | 2026-09-05 | Расписания смен и календаря нет. У чек-листа есть только окно времени, чтобы утром открывался утренний, а вечером вечерний | «не будем усложнять систему календарем... управляющие и так знают график». Метрика «пропущено» из MVP выпадает вместе с календарём | owner |
| D005 | 2026-09-05 | Офлайн-режима нет: ни IndexedDB, ни Service Worker | «оффлайн не нужен, забиваем» | owner |
| D006 | 2026-09-05 | QR-код станции перевыпускается в любой момент, вплоть до ежедневного. Предусмотрен вывод QR на экран планшета, а не только печать наклейки | «чтобы условная пиццерия могла хоть каждый день их менять... пригодится для привязки куара в вебе, на планшете». Ротация кода — единственная защита публичной ссылки при отказе от опознания (D001) | owner |
| D007 | 2026-09-05 | Иерархия: УК → страна → пиццерия → станция. Уровня «партнёр» нет | «у нас есть УК и есть страны. Стандартные чек листы делает УК. Страны могут пользоваться сервисом чтобы генерировать себе куаркоды для личных чек листов» | owner |
| D008 | 2026-09-05 | Разработка ведётся на личной машине владельца, боевое размещение — позже в контуре компании. На личной машине только демонстрационные данные, реальных выгрузок нет | «это разработка для команды международного направления... разработка да на личной машине, мне так проще. потом перекинем на сервера компании» | owner |
| D009 | 2026-09-05 | Документы проекта ведутся на русском, тексты продукта — на английском и русском | Владелец и команда читают документы по-русски; настройка Forge по умолчанию (английский) не соответствует этому проекту | auto |
| D010 | 2026-09-05 | Состав MVP: веб-сервис, где создаётся чек-лист для конкретной пиццерии конкретной страны, печатается QR станции, чек-лист заполняется по этому QR и результат виден в ленте. Аналитика, роли, тенанты партнёров, языки контента — после MVP | «что я вижу мвп: у нас есть веб сервис, в котором мы можем создать чек лист для конкретной пиццерии в конкретной стране. дальше уже доделываем аналитики, тенанты партнеров и прочее» | owner |
| D011 | 2026-09-05 | В чек-листе есть секции (группы пунктов со своим заголовком) и библиотека переиспользуемых блоков: блок заводится один раз и вставляется в любое число чек-листов, правка идёт в одном месте. Оба механизма входят в MVP | «блоки 1 раз завел и 10 раз переиспользуешь, да». Это главная ценность редактора: заведение чек-листов пачками превращается из копипасты в сборку | owner |
| D012 | 2026-09-05 | Чек-лист на экране заполнения визуально разделён на секции, а не идёт сплошным полотном | «визуально в самом чек листе это не должен быть скрижаль с данными, а все же немного разделяться» | owner |
| D013 | 2026-09-05 | Визуальный язык берётся из дизайн-системы Dodo P&L Service (направление «Реестр»): токены цвета, типографика Golos Text, малые радиусы, плотность, видимый фокус. Берётся только визуал — структура экранов своя. Токены скопированы в docs/forge/design/reference/ | «возьми за основу дизайн систему сервиса по работе с пнл. только визуал! не структуру» | owner |
| D014 | 2026-09-05 | Вход в админку в MVP есть, но минимальный: один аккаунт с паролем, без ролей и областей видимости | Без входа конструктор чек-листов открыт всему интернету; полноценные роли отложены за пределы MVP по D010 | owner |
| D015 | 2026-09-06 | Экранный эталон опубликован артефактом для сверки с коллегами: https://claude.ai/code/artifact/8f518909-fea6-4b71-8fad-b1658a8dbf6c — ссылка постоянная, страница обновляется по мере правок. Замечания коллег собираются на самой странице и читаются обратно в проект | «переведи это все в артефакт и закрепи его как постоянный... мне это надо чтобы отдать коллегам для сверки и обратной связи» | owner |
| D016 | 2026-09-06 | Лист QR печатается из браузера (`@media print`, `@page { size: A4 }`), серверный PDF не делаем | Ноль зависимостей и ноль расхождений между тем, что видно на экране, и тем, что вышло из принтера. Серверный PDF нужен для отправки почтой и архива — это не сценарий MVP | auto |
| D017 | 2026-09-06 | Версии стека: Next.js 16.3.4, Node 24 LTS, Drizzle ORM 0.45.2 (не кандидат 1.0), Tailwind 4.3.3, next-intl 4.14.2, Vitest 5 + Playwright 1.63, **TypeScript 6.0.3 — не 7.x** | TypeScript 7 (порт на Go) лишился Compiler API и несовместим с typescript-eslint 8.69: линтер с типами просто не запустится. Drizzle 1.0 не выпущен в стабильный тег даже авторами, а на нём держится вся схема данных | auto |
| D018 | 2026-09-06 | Промежуточного экрана-сводки перед отправкой заполнения нет: кнопка отправляет сразу | Чек-лист станции короткий, проваленный пункт уже подсвечен в моменте. Лишний экран — лишнее касание там, где мы боремся за каждое | auto |
| D019 | 2026-09-06 | Каркас проекта собирается руками, без CLI-генератора | Генераторы тянут аутентификацию, оплату и слои, которых у продукта нет; вычищать их дороже, чем написать пять конфигов | auto |
| D020 | 2026-09-06 | Перетаскивание пунктов мышью в MVP не делаем: порядок меняется стрелками и с клавиатуры | Перетаскивание на планшете мучительно, клавиатурные стрелки работают везде и уже заложены в экранный эталон | auto |
| D021 | 2026-09-06 | Защита публичной ссылки заполнения тремя мерами: ссылка позволяет только отправить заполнение и не отдаёт историю станции; частота отправок с одного кода ограничена; наклейка размещается в рабочей зоне, а не в зале | Следствие отказа от опознания (D001). Исследование рынка показало, что во всей категории продуктов вход обязателен — значит ответственность за защиту публичной точки записи полностью на нас | auto |
| D022 | 2026-09-06 | Бюджета на внешние сервисы нет. Всё, что нужно продукту, работает локально; платных зависимостей в MVP не появляется | «бюджета нет, строим в первую очередь мвп (мы обсуждали выше)» | owner |
| D023 | 2026-09-06 | Репозиторий заводится в личном аккаунте GarroV. Вопрос публичности вынесен владельцу отдельно: в открытый репозиторий уедут токены внутренней дизайн-системы Dodo P&L и документы внутреннего проекта | «репо личный открытый на гитхабе» — владелец назвал открытый, но публикация внутренних материалов компании необратима, поэтому подтверждается отдельно | owner |
| D024 | 2026-09-06 | В базу не ходит мимо `data` никто: драйвер импортирует только он. Но блоки, которым нужны свои запросы (справочник в `catalog`), берут из `data` схему и `getDb()` и пишут запросы у себя, а не заказывают функции в `data` | Иначе `data` собрал бы в себе CRUD всех блоков, то есть половину продукта, и стал бы узким местом параллельной стройки. Проверяемая машиной граница — запрет импорта драйвера — сохраняется | auto |
| D025 | 2026-09-06 | Язык страны в схеме называется `countries.locale`; контракт блока `catalog` приведён к этому имени, а не наоборот | Колонка одна и та же, код уже написан и покрыт тестами; переименование ради формулировки в документе — правка ради правки | auto |
| D026 | 2026-09-06 | В схему добавлена `stores.timezone`, окно времени чек-листа сравнивается с местным временем пиццерии | Без часового пояса утренний чек-лист в стране со сдвигом открывался бы среди дня. Прямое следствие D004: окно — единственный механизм выбора чек-листа, и оно обязано быть местным | auto |
| D027 | 2026-09-06 | Уязвимости esbuild внутри drizzle-kit терпим, drizzle-kit до 0.18 не откатываем | `npm audit --omit=dev` даёт ноль уязвимостей: в том, что попадает в работающий продукт, их нет вовсе. Дефект esbuild эксплуатируется только через запущенный дев-сервер самого esbuild, которого в проекте нет. Откат — ломающее изменение ради проблемы, которой у нас не существует | auto |
| D028 | 2026-09-06 | Шрифты Golos Text и IBM Plex Mono подключены через next/font с самохостингом; первая сборка в свежем клоне требует сети, чтобы их скачать | Самохостинг важнее: на кухне первый экран должен появляться быстро, а не ждать чужой сервер шрифтов. Требование сети на сборке приемлемо — сборка и так тянет зависимости | auto |
| D029 | 2026-09-06 | Расхождение экрана входа с эталоном решено в пользу продукта: заголовок — h1 с межстрочным из токена, эталон приведён к нему | Продукт следует правилам самой дизайн-системы, а в эталоне заголовок был обычным блоком и наследовал чужое межстрочное. Прав тот, кто следует системе | auto |
| D030 | 2026-09-06 | Относительный порог покрытия к приёмке блока catalog не применяется: общий показатель упал с 89,11% до 69,7%, но логика выросла до 91,5%, а вниз тянет впервые появившаяся разметка (25,3%), которая проверяется сквозными сценариями в браузере. Заведена T072 — считать покрытие раздельно | Порог заведён против молчаливой деградации. Здесь деградации нет: сравнивать 89% чистой логики с 69% смеси логики и разметки — сравнивать разные величины, и такой порог начал бы заворачивать блоки за то, что у продукта появились экраны | auto |
| D031 | 2026-09-06 | Экранный эталон приведён к спецификации: коды станций на макетах больше не содержат знаков 0 и 1 | Спека запрещает похожие знаки в коде станции, чтобы код нельзя было спутать при чтении с наклейки, а эталон рисовал их вопреки этому. Прав тот, кто задаёт правило, — эталон догоняет | auto |
| D038 | 2026-09-06 | Целевая площадка продукта — домашний сервер MUSPELHEIM: пилот живёт там, а личная машина владельца остаётся площадкой разработки (D008). Перенос в контур компании — за горизонтом MVP. Площадка общая: своя папка `C:\projects\dodo_qr_service`, своё имя compose-проекта, свои порты; чужие контейнеры не трогаем | «напомню что проект ставим на муспельхейм». MUSPELHEIM — площадка новых проектов: работает постоянно и не зависит от того, открыт ли ноутбук владельца, а чек-лист на кухне заполняют тогда, когда владельца нет за машиной | owner |
| D039 | 2026-09-06 | Репозиторий проекта — `GarroV/dodo_qr_service` на личном GitHub владельца, публичный. Беклог зеркалируется в Issues того же репозитория. Следствие принято сознательно: в задачи не попадает ничего, что не должно быть публичным, а находки по безопасности идут приватным advisory, а не задачей | «заводим на моем гитхаб, публичный репо». Так же заведены соседние проекты владельца (`dodo_pnl_service`, `dodo_audit_service`, `dodo_kb_bot` — все публичные): единая практика важнее разовой осторожности, а секретов в коде нет — `.env` вне git, значения в `.env.example` демонстрационные и в production вход с ними отказывает | owner |
| D034 | 2026-09-06 | Страница заполнения публикуется наружу через `tailscale funnel` на своём порту, а не переводом телефонов сотрудников в Tailscale. Корень `:443` на площадке занят чужим продуктом, поэтому берём отдельный порт | «tailscale funnel, свой порт». Продукт построен на том, что заполняющего не опознают (D001): требование ставить VPN на телефон каждого сотрудника вернуло бы администрирование доступов, которого продукт избегает. Плата — публичный маршрут заполнения, под него уже стоит задача T071 о строгом CSP | owner |
| D035 | 2026-09-06 | Переезд на MUSPELHEIM выполняется после волны 1, разом готовым MVP, а не поэтапно по ходу стройки. До переезда продукт живёт на машине владельца | «После волны 1, разом». Ставить сегодня значило бы показать снаружи админку без главного сценария — заполнения по QR: продукт выглядел бы готовым, не будучи им. Блоки `fill`, `qr`, `feed`, `library`, `demo` ещё не начаты | owner |
| D036 | 2026-09-06 | Публичным репозиторий становится только после того, как из истории вычищен PDF «Проектирование системы чек-листов Додо Пицца» — внутренний документ компании, лежащий в git с первого коммита (1,1 МБ). До этого репозиторий заводится приватным | Публикация необратима: документ уходит в поисковые кэши и клоны, и удаление файла потом уже ничего не отменяет. Само решение владельца о публичности (D039) этим не отменяется — меняется только порядок: сначала чистка, потом видимость | auto |
| D032 | 2026-09-06 | Выбор станции в редакторе показывает путь «Страна · Пиццерия · Станция», а не одно название станции, как на эталоне | Список станций в редакторе глобальный: «Кухня» есть в каждой пиццерии, и без пути методист выбирает вслепую. Эталон рисовал экран одной пиццерии, продукт работает со всей сетью | auto |
| D033 | 2026-09-06 | Сборка запекает значения переменных окружения: после смены секретов нужна пересборка, иначе запуск отвечает ошибкой о значении из примера. Учесть в раскатке | Найдено блоком editor при проверке живой сборки. На раскатке это выглядит как «поменяли пароль — продукт лёг», причём тесты и локальный запуск в режиме разработки проблемы не показывают | auto |
| D037 | 2026-09-06 | D036 отменено: репозиторий `GarroV/dodo_qr_service` переведён в публичные сразу, PDF концепции остаётся в истории, чистка не выполняется (T076 снята) | «репозиторий публичный, все документы что я тебе дал — не несут никакой сверх информации». Оценку чувствительности документов компании делает владелец, а не система: он их и принёс. Моя часть — показать, что именно уходит наружу, и это было показано до публикации | owner |
