<!-- Описание блока. Контракт здесь — источник истины для блок-агента. -->

# Block: core

## Назначение

Каркас проекта: приложение Next.js, база в Docker Compose, все проверки качества и единая команда прогона. Ничего продуктового здесь нет — этот блок делает так, чтобы остальные девять строились на общем основании и не спорили о стиле.

## API-контракт

Экспортирует не код, а окружение:

- `docker-compose.yml` — PostgreSQL 17 на порту 5433, том для данных.
- `.env.example` с именами переменных: `DATABASE_URL`, `PORT`, `ADMIN_PASSWORD_HASH`, `SESSION_SECRET`.
- `scripts/check` — исполняемый, запускает формат, линт, типы, тесты с покрытием, мёртвый код, границы; пишет `reports/*.junit.xml`.
- `.githooks/pre-push`, включается `git config core.hooksPath .githooks`; падает, когда инструмента нет.
- Приложение поднимается на `http://localhost:3100`.

## Зависимости

—

## Definition of Done блока

1. `docker compose up -d` поднимает базу, `psql "$DATABASE_URL" -c 'select 1'` отвечает.
2. `npm run dev` отдаёт страницу на 3100.
3. `scripts/check` проходит на пустом проекте и пишет отчёт в `reports/`.
4. Каждая из шести проверок **проверена на испорченной копии**: неформатированный файл, нарушение правила линта, ошибка типов, падающий тест, неиспользуемый экспорт, импорт мимо графа — каждая падает и печатает внятную причину.
5. `.npmrc` содержит `min-release-age=7`.
6. Смоук Tailwind 4 с токеном из `@theme inline` на реальной странице — цвет применился.
7. Смоук next-intl: язык страницы меняется по заголовку браузера без cookie.
8. Хук `pre-push` останавливает пуш при красной проверке (проверено намеренной поломкой).

## Статус

in_progress

## Журнал работы

- **2026-09-06.** Прочитаны контракт, принципы, спека, техплан, решения. Окружение: Node 24.17.0, npm 11.13.0, Docker 29.6.2 + Compose 5.3.1, клиент psql 16.14.
- **2026-09-06. Версии сверены с реестром npm, не по памяти.** Карантин `min-release-age=7` (npm разворачивает его в `before=2026-08-30`) блокирует часть версий из техплана — они опубликованы позже. Взяты новейшие разрешённые карантином (сделано сам, это решение по стеку):
  | план | фактически | почему |
  |---|---|---|
  | next 16.3.4 (2026-08-31) | **16.3.3** (2026-08-25) | 16.3.4 младше 7 дней; 16.3.3 — тот самый релиз с двумя critical-фиксами, 16.3.4 поверх него только багфиксы |
  | next-intl 4.14.2 (2026-09-01) | **4.14.1** (2026-08-28) | карантин |
  | vitest 5.0.0 (2026-09-03) | **4.1.11** (2026-08-18) | карантин; откат к 4.x заранее разрешён техпланом (раздел «Риски») |
  | playwright 1.63.0 (2026-09-04) | **1.62.1** (2026-07-30) | карантин |
  | eslint 10.10 (2026-09-04) | **10.9.1** (2026-08-24) | карантин |
  | typescript-eslint 8.69 (2026-08-31) | **8.68.0** (2026-08-24) | карантин; peer `typescript >=4.8.4 <6.1.0` — TS 6.0.3 подходит, 7.x нет (D017 подтверждён фактом) |
  | knip 6.34.0 | **6.33.0** | карантин |
  Без изменений: typescript 6.0.3, react/react-dom 19.2.8, tailwindcss + @tailwindcss/postcss 4.3.3, prettier 3.9.6, dependency-cruiser 18.2.0.

### Задачи

- **T001 — каркас, сделал сам.** Решения по стеку и раскладке файлов нельзя отдать без контракта, а контракта до установки зависимостей не было. Приложение отвечает на `http://localhost:3100` (dev, Turbopack), продакшен-сборка `next build` проходит. Каркас собран руками, без CLI-генератора (D019).
  - Next 16 при первом запуске сам переписал `tsconfig.json` (`jsx: react-jsx`, `allowJs`) и положил в корень свои `AGENTS.md` и `CLAUDE.md` — файлы удалены, генерация отключена флагом `agentRules: false`.
  - TS 6.0.3 объявляет `baseUrl` устаревшим (`TS5101`) — убран, `paths` работают и без него.
  - `tsc` поймал настоящую ошибку в `pickLocale`, которую тесты не видели: сужение типа предикатом не переносится на свойство объекта. Починено циклом с ранним возвратом.
- **T002 — база, сделал сам.** Мелочь на два файла. `docker compose -p dodo-qr-core up -d` поднимает PostgreSQL **17.11** на 5433, `psql "$DATABASE_URL" -c 'select 1'` отвечает.
  - **Найдено и обойдено:** на этой машине зависал любой доступ к реестру образов (`docker pull`, даже `docker manifest inspect`) при живой сети и рабочем демоне. Причина — залипший помощник учётных данных Docker Desktop: `docker-credential-desktop get` не отвечает вовсе. Обход без перезапуска чужих контейнеров: `DOCKER_CONFIG=<пустой каталог> DOCKER_HOST=unix:///Users/garva/.docker/run/docker.sock docker pull ...` — публичный образ тянется анонимно за секунды. Дальше compose берёт локальный образ обычной командой.
- **T003 — конфиги проверок; `eslint.config.mjs` сделал исполнитель.** Контракт был описан полностью (пресеты, список плагинов, требование точечных правил, критерий приёмки), работа механическая, ни миграций, ни доступа, ни денег — условия делегирования выполнены. Остальные конфиги (`tsconfig.json`, `vitest.config.ts`, `.prettierignore`, правка `knip.json`) сделал сам: там по паре строк.
  - Порог покрытия абсолютным числом не задаётся: принципы требуют относительного. `vitest` пишет `reports/coverage/coverage-summary.json`, приёмка сравнивает с прошлым прогоном. Первый замер: строки 81.48%, ветки 81.25%.
  - `knip` считал мёртвыми `tailwindcss` (виден только из CSS) и `dependency-cruiser` (зовётся из `scripts/check`) — внесены в `ignoreDependencies`. Зависимость `globals` оказалась не нужна и удалена, экспорт `LOCALES` без потребителей убран.
  - `prettier` требовал переформатировать все документы стройки, включая чужие `tasks.md` и `progress.md`. Заведён `.prettierignore` на `docs/`, `tasks.md`, `progress.md`.
- **T004 — единая команда прогона, сделал сам.** Скрипт уже был; добавлена седьмая роль — сквозные сценарии, чтобы появился `reports/playwright.junit.xml`.
- **T007 — смоуки рисков, сделал сам.** Это разведка неизвестного, а не механическая работа: заранее описанного контракта у неё нет.
  - **Tailwind 4.3.3 + Turbopack — работает.** `@theme inline { --color-accent: var(--accent); }` поверх `docs/forge/design/reference/tokens.css`; браузер отдаёт `rgb(31, 78, 156)`, это `--accent: #1F4E9C` из токенов. Проверено на продакшен-сборке. Turbopack отключать не понадобилось. Токены не копировались: `globals.css` импортирует файл эталона, источник правды остаётся один.
  - **next-intl 4.14.1 в Next 16 — работает, и `proxy.ts` для этого не нужен вовсе.** Локаль в URL продукту не нужна, поэтому взят режим без маршрутизации локали: `getRequestConfig` читает `Accept-Language` через `headers()`. Русский телефон получает русский текст, английский — английский, французский — язык по умолчанию (английский); `Set-Cookie` в ответе нет, cookie в контексте браузера ноль. Риск техплана снят не обходом, а тем, что связка с `proxy.ts` этому продукту не требуется. Если `proxy.ts` понадобится следующему блоку (ограничение частоты на публичном маршруте), его совместимость надо будет проверять отдельно — этот смоук про неё ничего не говорит.
- **Длительность полного прогона `./scripts/check` — 11 секунд** (зелёный прогон; из них сквозные сценарии со сборкой ~5 с). Число для следующих агентов: набор дешёвый, гонять целиком не страшно.
