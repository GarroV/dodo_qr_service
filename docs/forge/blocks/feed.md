<!-- Описание блока. Контракт здесь — источник истины для блок-агента. -->

# Block: feed

## Назначение

Лента заполнений и карточка одного заполнения: что заполнено, когда, сколько заняло и что пошло не так. Без оценок и цветных ярлыков — только факты.

## API-контракт

```ts
/admin/feed?country=&store=&station=&period=today|week|month
/admin/feed/<id>?country=&store=&station=&period=   // фильтры доезжают до карточки и возвращают в ту же ленту
```
Карточка показывает пункты в той версии, по которой заполняли, — снимком, а не текущим чек-листом.

Период задаётся тремя вариантами эталона, а не парой дат `from`/`to`: границы всё равно считает сервер в часовом поясе пиццерии, и пара дат в адресе позволяла бы вручную задать окно, которого на экране нет (см. решения блока).

## Зависимости

`data`, `auth`

## Definition of Done блока

1. Фильтры по стране, пиццерии, станции и периоду работают вместе.
2. Три показателя за выбранный день считаются по тем же данным, что и лента.
3. Карточка показывает ответ и время по каждому пункту и комментарии к проваленным.
4. После правки и публикации новой версии чек-листа старая карточка не меняется — сквозной тест.
5. Пустое состояние объясняет, что делать дальше.
6. Экраны совпадают с эталонами `feed.html` и `submission.html`.

## Устройство блока

| Файл | Что внутри |
|---|---|
| `src/blocks/feed/routes.ts` | `FEED_PATH`, `submissionPath(id)` — адреса блока в одном месте |
| `src/blocks/feed/view.ts` | Разбор фильтров из адреса и сборка адресов (`parseFeedView`, `feedHref`, `submissionHref`) |
| `src/blocks/feed/zone.ts` | Часовые пояса: смещение, местная дата, начало местных суток |
| `src/blocks/feed/period.ts` | Три периода эталона в границы времени и `relativeDay` (сегодня / вчера / раньше) |
| `src/blocks/feed/selection.ts` | Согласование фильтров между собой и выбор пояса экрана |
| `src/blocks/feed/options.ts` | Свой запрос справочника для выпадающих списков и пояс станции |
| `src/blocks/feed/failures.ts` | Сколько пунктов провалено в каждом заполнении — дочитывание по идентификаторам строк ленты |
| `src/blocks/feed/versions.ts` | Дата публикации версии для оговорки о снимке |
| `src/blocks/feed/outcome.ts` | Итог заполнения одной величиной: провал критичного → провал → без ответа → всё выполнено |
| `src/blocks/feed/metrics.ts` | Три показателя по массиву строк ленты |
| `src/blocks/feed/format.ts`, `text.ts`, `checklist-link.ts` | Длительность «3:24», выбор языка заголовка, ссылка в редактор |
| `src/blocks/feed/model.ts` | Модель обоих экранов: что посчитано для разметки |
| `src/blocks/feed/ui/build-model.ts` | Сборка моделей: фильтры → слой доступа → показатели → снимок |
| `src/blocks/feed/ui/Feed*.tsx`, `OutcomeTag.tsx` | Экран ленты по эталону `feed.html` |
| `src/blocks/feed/ui/Submission*.tsx`, `AnswersCard.tsx` | Карточка по эталону `submission.html` |
| `src/app/admin/feed/` | Маршруты: лента, карточка, «не найдено» |
| `e2e/feed.spec.ts` | Сквозные сценарии, включая главный: новая версия не меняет старую карточку |

## Решения, принятые внутри блока

| Решение | Почему |
|---|---|
| Три показателя считаются функцией `computeMetrics` по тому же массиву строк, который показан в ленте, а не отдельным запросом с агрегатами | Требование T045 буквально. Два запроса с независимой фильтрацией разъезжаются на первой же правке, и управляющий видит «14 заполнений» над лентой из тринадцати — причём без единой ошибки в коде, просто от расхождения условий |
| Строка ленты различает провал критичного пункта, провал обычного и пункт без ответа; «Всё выполнено» показывается, только когда провалов нет вовсе | `listSubmissions` отдаёт лишь `failedCriticalCount`, и заполнение с непройденным НЕкритичным пунктом получило бы зелёную метку «Всё выполнено». Это ложь на главном экране управляющего — хуже отсутствия метки |
| Число проваленных пунктов дочитывается вторым узким запросом по идентификаторам уже отобранных строк (`failures.ts`), а не собственной копией запроса ленты | Копия запроса означала бы второй набор условий отбора — ровно то, чего требует не допускать T045. Здесь набор строк по-прежнему целиком за слоем доступа: дочитывание идёт по первичному ключу и правилом провала берёт `isFailed` из `data`, то есть лента и карточка считают провал одинаково |
| Своими запросами читается и справочник для фильтров (`options.ts`), и дата публикации версии (`versions.ts`) | D024: блоки, которым нужны свои запросы, берут из `data` схему и `getDb()` и пишут их у себя. Заказывать в `data` списки стран и пиццерий значило бы собирать там CRUD всех блоков |
| Период — три варианта эталона (сегодня / 7 дней / месяц), а не произвольная пара дат | Эталон рисует именно список, а не выбор дат; «месяц» — последние 30 суток, а не календарный месяц, иначе третьего числа управляющий видит почти пустой экран |
| Границы суток считаются в часовом поясе пиццерии, а время каждой строки показывается в поясе ЕЁ пиццерии | Сотрудник видел на кухне местное время, и управляющий обязан видеть то же самое. В сети из нескольких стран (эталон рисует Казахстан и Узбекистан) пояс сервера превратил бы утренний чек-лист соседней страны в ночной |
| Когда пиццерия не выбрана, а пояса у пиццерий фильтра разные, период считается по поясу площадки — и экран подписывает, по какому именно | Одного правильного ответа здесь не существует, а молчаливое допущение о том, что такое «сегодня», — худший вариант: он невидим. Подпись под фильтрами делает допущение явным |
| Выбранная станция задаёт пояс экрана, даже если пиццерия в фильтре осталась «Все» | Станция однозначно принадлежит одной пиццерии: считать её сутки по поясу площадки значило бы игнорировать уже сделанный выбор |
| Пиццерия чужой страны и станция чужой пиццерии из фильтра выпадают (`resolveSelection`) | Иначе несогласованные фильтры складываются в заведомо пустую ленту, и экран показывает «ничего не заполняли» там, где заполнений просто не может быть по построению |
| Пустое состояние различает три причины: нет станций, не заполняли ни разу по этим фильтрам, не заполняли в этом периоде | Критерий готовности требует объяснить, что делать дальше, а совет невпопад («напечатайте QR-коды» там, где надо расширить период) хуже отсутствия совета. Второй запрос ради этого уходит ТОЛЬКО при пустой ленте и берёт одну строку |
| Предел выдачи ленты (200) задан в блоке явно, и экран говорит, когда упёрся в него | Унаследованное умолчание слоя доступа не позволяет отличить «столько и было» от «дальше обрезано»: часть истории молча выглядела бы всей историей |
| Фильтры — обычная GET-форма, применяемая по выбору из клиентского компонента, с кнопкой отправки в `<noscript>` | На эталоне кнопки «Показать» нет, а состояние экрана обязано жить в адресе (ссылкой на «Кухню за неделю» делятся в чате). Без JavaScript экран остаётся рабочим |
| Метка итога — один компонент `OutcomeTag` на оба экрана | Лента и карточка обязаны называть один и тот же итог одними словами; две копии таблицы соответствий разъезжаются на первой правке словаря |
| Каркас админки (`AdminNav`, `AdminShell`) снова скопирован в блок | Границы модулей запрещают ленте зависеть от `catalog`, `editor` и `qr`, а общего места для каркаса в проекте нет. Та же цена уже заплачена блоками `editor` и `qr` |

## Журнал работы

| Задача | Кем | Что сделано |
|---|---|---|
| T044 | сам | Фильтры страны, пиццерии, станции и периода: разбор адреса (`view.ts`), согласование выбора между собой (`selection.ts`), периоды в границы времени по часовому поясу (`zone.ts`, `period.ts`), справочник для списков (`options.ts`). Логика фильтров — не механическая работа: здесь решается, что считать «сегодня» у сети в разных поясах, поэтому делал сам. **Отрицательный прогон:** фильтр по станции молча потерян (`const stationId = null`) — упало 10 проверок, включая «страна, пиццерия, станция и период работают вместе» |
| T045 | сам | Три показателя (`metrics.ts`) считаются по массиву строк, который экран и показывает; предел выдачи задан явно, экран говорит, когда упёрся в него. **Отрицательный прогон:** показатели переведены на отдельный запрос без периода — упали обе проверки (`expected 8 to be 2`, `expected { submissionCount: 11 } to strictly equal { submissionCount: 0 }`) |
| T046 | сам | Карточка: пункты из снимка заполнения, ответ, время и комментарий по каждому, дата публикации версии (`versions.ts`), пункт без ответа отличается от выполненного. Заодно закрыт разрыв контракта: `listSubmissions` отдаёт только критичные провалы, и заполнение с непройденным НЕкритичным пунктом получало бы метку «Всё выполнено» — добавлено дочитывание провалов по идентификаторам строк (`failures.ts`) с правилом провала из `data`. **Отрицательный прогон:** ветка «провал без критичных» убрана из `outcomeOf` — упали три проверки (`expected { kind: 'ok' } to strictly equal { kind: 'failed', count: 1 }`) |
| T047 | сам | Сквозной сценарий `e2e/feed.spec.ts`: правка и публикация новой версии не меняют сохранённую карточку. **Здесь же найден молчаливый гейт — свой собственный.** Первая редакция сценария публиковала новую версию (вставка строки) и проходила ЗЕЛЁНОЙ на нарочно испорченном коде, где карточка читает пункты из версии, а не из снимка: заполнение ссылается на прежнюю версию, и её содержимое никто не трогал. То есть сценарий проверял только неизменяемость версий — первую опору D002 — и молчал о второй. Добавлен шаг «переписать саму версию, по которой заполняли»: на той же порче сценарий упал (`Expected -12 / Received +3` — вместо трёх исходных пунктов карточка показала один новый), после восстановления снова зелёный |
| T048 (лента) | сам | Экран ленты по эталону `feed.html`: каркас, фильтры (GET-форма, применяется по выбору, с кнопкой в `<noscript>`), три показателя, таблица, пустое состояние трёх видов. Делал сам вынужденно: запуск исполнителей был отклонён сторожем лимита подписки (см. строку о волне ниже) |
| T048 (карточка) | исполнитель | Карточка по эталону `submission.html`: четыре факта, оговорка о снимке, секции и строки ответов с метками, значениями, временем и комментариями (`SubmissionScreen.tsx`, `SubmissionFacts.tsx`, `AnswersCard.tsx`, `SubmissionNotFound.tsx`). Готовый контракт (модель, ключи словаря, разбиение на файлы, тестовые идентификаторы), механическая работа, миграций и доступа не касается — ровно тот случай, когда задача обязана уходить исполнителю |
| Приёмка работы исполнителя | сам | Проверял руками, а не по отчёту: перечитал все четыре файла, прогнал типы, линт, мёртвый код, границы модулей, настоящую сборку `next build` и открыл экраны в браузере рядом с эталоном. Найдено и исправлено мной: (1) пункт показывал ДВА пояснения подряд — посчитанный диапазон и подсказку методиста, которая его же и повторяет («160–180 160–180 °C»), тогда как на эталоне пояснение одно; теперь подсказка побеждает, диапазон остаётся запасным вариантом; (2) на экране «заполнение не найдено» один и тот же заголовок стоял и в шапке, и в карточке — в шапке теперь раздел. Остальное принято как есть: разбиение, стиль и все ветки модели отрисованы |
| Сверка с эталоном в живом браузере | сам | Снимки эталонов и живых экранов сняты рядом (`reports/feed-block/*.png`, вне git). Расхождения, найденные глазами и исправленные: список станций показывал несколько одинаковых «Кухня» — без выбранной пиццерии станция теперь названа путём (та же причина, что в D032); подпись показателя не согласовывалась с числом («4 заполнений» вместо «4 заполнения») — переведена на множественные формы; строка о часовом поясе показывалась всегда, хотя на эталоне её нет, — теперь только когда пояс действительно неоднозначен |
| Волна и лимит подписки | сам | Оба исполнителя (лента и карточка) были запущены одновременно и **отклонены сторожем лимита**: «окно seven_day израсходовано на 75%… в волне уже 2». Повтор через десять минут пропустил одного — им сделана карточка; лента написана мной. Записано сюда по требованию сторожа: если стройка оборвётся по лимиту, причина должна остаться в файле состояния, а не только в исчезнувшем уведомлении |

## Статус

done
