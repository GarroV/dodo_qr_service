/**
 * Адреса входа. Лежат отдельно от охраны маршрутов, потому что нужны и ей,
 * и серверным действиям формы: один и тот же путь, записанный в двух местах, разъезжается.
 *
 * `/admin/login` — единственный адрес под `/admin`, доступный без сессии. Он и живёт вне
 * каталога `src/app/admin/`, чтобы не попасть под охрану `src/app/admin/layout.tsx`.
 */
export const LOGIN_PATH = "/admin/login";

/** Куда попадает вошедший. Первый экран админки. */
export const ADMIN_HOME_PATH = "/admin";
