-- Снятие чек-листа с работы вместо стирания истории (T092).
-- Удалить чек-лист, по которому уже заполняли, нельзя: заполнения ссылаются на его
-- версии и хранят снимок пунктов (принцип 3, D002). Такой чек-лист уходит из списка и
-- перестаёт открываться на станции, а лента продолжает показывать его историю.
ALTER TABLE "checklists" ADD COLUMN "archived_at" timestamp with time zone;