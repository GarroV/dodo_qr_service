// Выполняется в каждом воркере vitest до тестов: подменяет адрес базы на тестовый.
// Без этого слой доступа в тестах пошёл бы в рабочую базу разработчика и стёр её данные
// на первом же прогоне, который пересоздаёт схему.
import { testDatabaseUrl } from "./db";

process.env["DATABASE_URL"] = testDatabaseUrl();
