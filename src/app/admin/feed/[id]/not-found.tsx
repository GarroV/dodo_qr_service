import { SubmissionNotFound } from "@/blocks/feed/ui/SubmissionNotFound";

/** Заполнения с таким идентификатором нет: объяснение и возврат в ленту. */
export default function SubmissionNotFoundPage() {
  return <SubmissionNotFound />;
}
